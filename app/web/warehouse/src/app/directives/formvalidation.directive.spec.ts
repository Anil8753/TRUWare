import { FormvalidationDirective } from './formvalidation.directive';

describe('FormvalidationDirective', () => {
  it('should create an instance', () => {
    const directive = new FormvalidationDirective();
    expect(directive).toBeTruthy();
  });
});
